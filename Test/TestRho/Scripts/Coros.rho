fun Wait(millis)
	start = TimeNow()
	self.Delay(millis)
	end = TimeNow()
	d = TimeNow() - start
	Print(d)

fun WaitForOther()
	start = TimeNow()
	self.Wait(Wait(1))
	d = TimeNow() - start
	Print(d)

Wait(200)

//WaitForOther()

`clear`


