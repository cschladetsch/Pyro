using Pyro.Network;

namespace MyNamespace.Agent {
    interface IFooAgent
        : IAgentBase<IFooAgent> {
        string GetName();
        bool SetName(string name);
        int Sum(int a, int b);
    }
}
